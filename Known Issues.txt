1. CanAddItemToInventory() won't add duplicate item to stack when there's no availabe inventory slot

2. When switching to other UI, ingame UI will be set as inactive, which will pause its script, causing skillcooldownImage not corrosponding to their real cd